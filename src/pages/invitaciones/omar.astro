---
import InvitacionLayout from '../../layouts/InvitacionLayout.astro';

const props = {
  nombre: 'Omar',
  tipo: '18-anos' as const,
  titulo: 'Mis 18 Años',
  subtitulo: 'Omar',
  fecha: '24 DE MAYO DE 2026',
  fechaData: '2026-05-24',
  hora: '22:00',
  lugar: 'Salón Sky',
  direccion: 'Av. del Puerto 456, Ciudad',
  padres: ['Roberto Sánchez', 'Laura Gutiérrez'],
  frase: '"Un nuevo capítulo comienza. 18 años listos para escribir mi propia historia."',
  dresscode: 'ELEGANTE - FORMAL',
  dresscodeColores: ['navy', 'silver', 'white', 'black'],
  instagramHashtag: 'MIS18OMAR',
  invitacionId: 'omar',
  whatsappMensaje: 'Hola, me contacto desde la tarjeta Omar, me gustó mucho, quisiera más información',
  imagenes: [
    { src: '/invitaciones/omar/img/boy1.jpg', alt: 'Omar', nombre: 'OMAR SÁNCHEZ', subtitle: '18 años' },
    { src: '/invitaciones/omar/img/boy2.jpg', alt: 'Omar', nombre: 'OMAR SÁNCHEZ', subtitle: 'Sus momentos' },
    { src: '/invitaciones/omar/img/boy3.jpg', alt: 'Omar', nombre: 'OMAR SÁNCHEZ', subtitle: 'Lista para celebrar' },
  ],
  itinerarios: [
    { hora: '22:00 HS', actividad: 'Recepción', detalle: 'Bienvenida a los invitados', icono: '🥂' },
    { hora: '22:30 HS', actividad: 'Inicio de la Fiesta', detalle: 'Bienvenida y brindis', icono: '🎤' },
    { hora: '00:00 HS', actividad: 'Corte de Torta', detalle: 'El momento más esperado', icono: '🎂' },
    { hora: '02:00 HS', actividad: 'Cierre', detalle: 'Fin de la celebración', icono: '🎊' },
  ],
  datosBancarios: {
    banco: 'Banco Ciudad',
    alias: 'OMAR.18',
    titular: 'Omar Sánchez',
  },
};
---

<InvitacionLayout 
  title={`${props.nombre} - ${props.titulo}`}
  description={`Invitación digital para los 18 años de ${props.nombre}`}
  nombre={props.nombre}
  tipo={props.tipo}
  invitacionId={props.invitacionId}
>
  <div id="notificaciones"></div>

  <audio id="audio" src="/invitaciones/omar/audio/audio.mp3" loop></audio>

  <button id="btn-musica" class="btn-musica" onclick="toggleMusica()">
    <i class="fas fa-play"></i>
  </button>

  <section id="hero" class="hero">
    <div class="hero-lines"></div>
    <div class="hero-content">
      <p class="hero-date">24 DE MAYO DE 2026</p>
      <p class="hero-subtitle">Mis 18 Años</p>
      <h1 class="hero-title">Omar</h1>
      <p class="hero-number">18</p>
      <p class="hero-name">SÁNCHEZ</p>
      <a href="#padres" class="hero-btn" onclick="iniciarMusica(); document.getElementById('fotos').scrollIntoView({behavior: 'smooth'}); return false;">Descubrir</a>
    </div>
  </section>

  <section id="padres" class="padres">
    <div class="padres-container">
      <p class="padres-text">Con la bendicíon de</p>
      <p class="padres-names">{props.padres[0]} <span>&</span> {props.padres[1]}</p>
    </div>
  </section>

  <section id="fotos" class="fotos">
    <h2 class="fotos-title">El cumpleañero</h2>
    <div class="slider-container">
      <div class="slider-wrapper" id="sliderWrapper">
        {props.imagenes.map((img, index) => (
          <div class="slider-slide">
            <img src={img.src} alt={img.alt} loading={index === 0 ? 'eager' : 'lazy'} />
            <div class="slider-overlay">
              <p class="slider-name">{img.nombre}</p>
              <p class="slider-subtitle">{img.subtitle}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
    <div class="slider-nav">
      {props.imagenes.map((_, index) => (
        <div class={`slider-dot ${index === 0 ? 'active' : ''}`} onclick={`goToSlide(${index})`}></div>
      ))}
    </div>
  </section>

  <section id="fecha" class="fecha">
    <div class="fecha-container">
      <div class="fecha-icon">📅</div>
      <p class="fecha-title">Cuando</p>
      <p class="fecha-text">24 de Mayo</p>
      <p class="fecha-detail">A las 22:00 horas</p>
    </div>
  </section>

  <section id="countdown" class="countdown" data-fecha={props.fechaData} data-hora={props.hora}>
    <h2 class="countdown-title">La cuenta regresiva</h2>
    <div class="countdown-timer">
      <div class="count-block">
        <div class="count-box">
          <span class="count-number" id="days">00</span>
        </div>
        <span class="count-label">Días</span>
      </div>
      <div class="count-block">
        <div class="count-box">
          <span class="count-number" id="hours">00</span>
        </div>
        <span class="count-label">Horas</span>
      </div>
      <div class="count-block">
        <div class="count-box">
          <span class="count-number" id="minutes">00</span>
        </div>
        <span class="count-label">Min</span>
      </div>
      <div class="count-block">
        <div class="count-box">
          <span class="count-number" id="seconds">00</span>
        </div>
        <span class="count-label">Seg</span>
      </div>
    </div>
  </section>

  <section id="frase" class="frase">
    <div class="frase-container">
      <p class="frase-text">{props.frase}</p>
      <p class="frase-autor">— Omar</p>
    </div>
  </section>

  <section id="ubicacion" class="ubicacion">
    <div class="ubicacion-container">
      <div class="ubicacion-icon">📍</div>
      <p class="ubicacion-titulo">Donde</p>
      <p class="ubicacion-lugar">{props.lugar}</p>
      <p class="ubicacion-detalle">{props.direccion}</p>
      <a href="#" onclick="mostrarDemo(); return false;" class="ubicacion-btn">Ver en Mapa</a>
    </div>
  </section>

  <section id="dresscode" class="dresscode">
    <div class="dresscode-container">
      <h2 class="dresscode-title">Dress Code</h2>
      <p class="dresscode-text">{props.dresscode}</p>
      <div class="dresscode-colors">
        <div class="color-swatch navy"></div>
        <div class="color-swatch silver"></div>
        <div class="color-swatch white"></div>
        <div class="color-swatch black"></div>
      </div>
    </div>
  </section>

  <section id="itinerario" class="itinerario">
    <div class="itinerario-container">
      <h2 class="itinerario-title">Itinerario</h2>
      {props.itinerarios.map(item => (
        <div class="itinerario-item">
          <div class="itinerario-icon">{item.icono}</div>
          <div class="itinerario-content">
            <p class="itinerario-hora">{item.hora}</p>
            <p class="itinerario-actividad">{item.actividad}</p>
            <p class="itinerario-detalle">{item.detalle}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section id="regalos" class="regalos">
    <div class="regalos-container">
      <div class="regalos-icon">🎁</div>
      <h2 class="regalos-titulo">Regalos</h2>
      <p class="regalos-texto">Tu presencia es el mejor regalo. Si deseas hacerme un presente, puedes hacerlo en efectivo o transferir a la cuenta que te proporcionaré al confirmar.</p>
      <button class="regalos-btn" onclick="openModal()">Ver Datos Bancarios</button>
    </div>
  </section>

  <section id="instagram" class="instagram">
    <div class="instagram-container">
      <div class="instagram-icon">📸</div>
      <h2 class="instagram-titulo">Comparte tus fotos</h2>
      <p class="instagram-texto">Etiquetame en tus fotos de la fiesta para que pueda revivir cada momento especial.</p>
      <p class="instagram-usuario">@omar_sanchez</p>
      <p class="instagram-hashtag-titulo">Usa el hashtag</p>
      <p class="instagram-hashtag">#{props.instagramHashtag}</p>
    </div>
  </section>

  <section id="galeria" class="instagram">
    <div class="instagram-container">
      <h2 class="instagram-titulo">GALERÍA DE INVITADOS</h2>
      <p class="instagram-texto">Comparte tu foto de la fiesta conmigo</p>
      <div class="galeria-grid" id="galeriaGrid"></div>
      <div class="galeria-upload">
        <input type="file" id="galeriaInput" accept="image/*" multiple hidden />
        <button class="instagram-btn" onclick="document.getElementById('galeriaInput').click()">
          📷 Subir mis fotos
        </button>
        <div class="galeria-preview" id="galeriaPreview" style="display: none">
          <div class="galeria-preview-grid" id="galeriaPreviewGrid"></div>
          <div class="galeria-preview-buttons">
            <button class="galeria-submit" id="btnEnviar" onclick="subirFotos()">Enviar todas</button>
            <button class="galeria-cancel" onclick="cancelarFotos()">Cancelar</button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="especial" class="especial">
    <div class="especial-container">
      <div class="especial-icon">🎉</div>
      <p class="especial-texto">Celebra</p>
      <p class="especial-texto">La Vida</p>
    </div>
  </section>

  <section id="confirmar" class="confirmar">
    <div class="confirmar-container" id="confirmarNormal">
      <div class="confirmar-icon">💬</div>
      <h2 class="confirmar-titulo">Confirmar Asistencia</h2>
      <p class="confirmar-fecha">Confirma antes del 10 de Mayo</p>
      <a href="#" onclick="mostrarDemo(); return false;" class="confirmar-btn">Confirmar por WhatsApp</a>
    </div>
    <div class="confirmar-container" id="confirmarToken" style="display: none">
      <div class="confirmar-icon">🎉</div>
      <h2 class="confirmar-titulo">Confirmar Asistencia</h2>
      <p class="confirmar-texto" id="confirmarNombre">Hola, te invito a mi fiesta</p>
      <div class="confirmar-form" id="confirmarForm">
        <p class="confirmar-cantidad">¿Cuántas personas vienen?</p>
        <div class="confirmar-numeros">
          <button type="button" onclick="cambiarCantidad(-1)">-</button>
          <span id="cantidadPersonas">1</span>
          <button type="button" onclick="cambiarCantidad(1)">+</button>
        </div>
        <p class="confirmar-info" id="confirmarInfo">Somos 1 persona</p>
        <div class="confirmar-botones">
          <button type="button" class="confirmar-btn si" onclick="confirmarAsistencia('si')">
            <i class="fas fa-check"></i> Asistiré
          </button>
          <button type="button" class="confirmar-btn no" onclick="confirmarAsistencia('no')">
            <i class="fas fa-times"></i> No podré asistir
          </button>
        </div>
      </div>
      <div class="confirmar-exito" id="confirmarExito" style="display: none">
        <i class="fas fa-check-circle"></i>
        <p>¡Gracias por confirmar!</p>
      </div>
    </div>
  </section>

  <section id="footer" class="footer">
    <div class="footer-container">
      <div class="footer-icon">🎊</div>
      <p class="footer-invitacion">Invitación</p>
      <p class="footer-de">de</p>
      <p class="footer-nombre">OMAR</p>
      <p class="footer-texto">TE ESPERO</p>
      <p class="footer-hashtag">#{props.instagramHashtag}</p>
      <div class="footer-markentas">
        <a href="/invitaciones#18-anos" class="footer-markentas-link">← Volver a los Diseños</a>
      </div>
      <div class="footer-panel">
        <a href="/invitaciones/omar/admin/index.html" class="footer-panel-link">PANEL</a>
      </div>
    </div>
  </section>

  <a 
    href={`https://api.markentas.com/api/whatsapp?src=${encodeURIComponent(props.whatsappMensaje)}`}
    target="_blank"
    class="whatsapp-float"
    id="whatsappBtn"
  >
    <i class="fab fa-whatsapp"></i>
  </a>

  <button class="saludos-float" id="saludosBtn" onclick="abrirModalSaludos()">
    <i class="fas fa-comments"></i>
    <span class="saludos-badge" id="saludosBadge">0</span>
  </button>

  <div id="modalSaludos" class="modal" onclick="closeModalSaludos(event)">
    <div class="modal-content saludos-modal">
      <button class="modal-cerrar" onclick="closeModalSaludos()">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M1 1L13 13M1 13L13 1" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
        </svg>
      </button>
      <h3 class="modal-titulo">Libro de Visitas</h3>
      <div class="saludos-lista" id="saludosLista">
        <p class="saludos-vacio">Cargando...</p>
      </div>
      <div class="saludos-form">
        <input type="text" id="saludoNombre" placeholder="Tu nombre" class="saludos-input" />
        <textarea id="saludoMensaje" placeholder="Escribe tu mensaje..." class="saludos-textarea" rows="2"></textarea>
        <button class="saludos-btn" onclick="enviarSaludo()">Enviar</button>
      </div>
    </div>
  </div>

  <div id="modal" class="modal" onclick="closeModal(event)">
    <div class="modal-content">
      <button class="modal-cerrar" onclick="closeModal()">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M1 1L13 13M1 13L13 1" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
        </svg>
      </button>
      <h3 class="modal-titulo">Datos de la Cuenta</h3>
      <div class="modal-datos">
        <p class="modal-banco">{props.datosBancarios.banco}</p>
        <p class="modal-alias">{props.datosBancarios.alias}</p>
        <p class="modal-titular">Titular: {props.datosBancarios.titular}</p>
      </div>
      <button class="modal-cerrar" onclick="closeModal()">Cerrar</button>
    </div>
  </div>

  <div id="lightbox" class="lightbox">
    <button class="lightbox-cerrar" onclick="cerrarLightbox()">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M1 1L13 13M1 13L13 1" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </button>
    <button class="lightbox-nav lightbox-prev" onclick="navegarLightbox(-1)">&#10094;</button>
    <button class="lightbox-nav lightbox-next" onclick="navegarLightbox(1)">&#10095;</button>
    <img id="lightbox-img" class="lightbox-img" src="" alt="Foto" />
  </div>


  <script>
    window.addEventListener('scroll', function () {
      const scrollTop = window.scrollY || document.documentElement.scrollTop;
      const docHeight = document.documentElement.scrollHeight;
      const winHeight = window.innerHeight;
      const whatsappBtn = document.getElementById('whatsappBtn');

      if (whatsappBtn) {
        if (scrollTop + winHeight >= docHeight - 200) {
          whatsappBtn.classList.add('visible');
        } else {
          whatsappBtn.classList.remove('visible');
        }
      }
    });
  </script>
</InvitacionLayout>
